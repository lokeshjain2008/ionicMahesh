{
  "rules": {
    "menu": {
      "$id": {
        ".validate": "newData.hasChildren(['name', 'price', 'desc']) && newData.child('name').val() != null && newData.child('price').val() > 0 && newData.child('desc').child('lenght').val() <= 140",
        "name": {
          ".validate": "newData.isString()"
        },
        "price": {
          ".validate": "newData.isNumber()"
        },
        "desc": {
          ".validate": "newData.isString()"
        },
        "$other": {
          ".validate": "false"
        },
        ".read": "auth != null",
        ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'father'"
      }
    },
    "users": {
      "$userId": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $userId"
      }
    },
    "canteen": {
      ".validate": "newData.hasChildren(['name', 'owner', 'accountNumber', 'desc'])",
      "name": {
        ".validate": "newData.isString()"
      },
      "owner": {
        ".validate": "newData.isString()"
      },
      "accountNumber": {
        ".validate": "newData.isNumber()"
      },
      "desc": {
        ".validate": "newData.isString()"
      },
      "$other": {
        ".validate": "false"
      },
      ".read": "true",
      ".write": "auth != null && auth.uid == 'google:116068349236349761960'"
    },
    "settings": {
      ".validate": "newData.child('message').val().length <= 140",
      ".write": "data.child('readOnly').val() == 1"
    }
  }
}
